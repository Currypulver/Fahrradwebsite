{
  "name": "fahrradwebsite",
  "version": "1.0.0",
  "private": true,
  "description": "ERGO Fahrradkasko Landingpage – statische Seite mit Framer Motion (Vanilla-Bundle)",
  "scripts": {
    "build:motion": "esbuild src/motion-entry.js --bundle --minify --format=iife --global-name=Motion --outfile=assets/motion.js"
  },
  "dependencies": {
    "framer-motion": "^12.42.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
