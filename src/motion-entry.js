/* Framer Motion (Vanilla-Einstieg) → wird per esbuild zu assets/motion.js gebündelt
   und stellt window.Motion mit animate/scroll/inView/stagger/spring bereit. */
export { animate, scroll, inView, stagger, spring, transform, frame } from "framer-motion/dom";
